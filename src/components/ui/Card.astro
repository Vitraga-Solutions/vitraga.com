---
const { isUnderline } = Astro.props;
---

{
  isUnderline ? (
    <div class="rounded-[45px] border border-dark shadow-[0px_5px_0px_#191a23] hover:scale-105 duration-200 ease-in-out">
      <slot />
    </div>
  ) : (
    <div class="rounded-[45px]">
      <slot />
    </div>
  )
}
